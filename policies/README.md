# policies

Deny-all Rego scaffolds for future OPA integration. No runtime caller exists yet. Actor, action, resource, arguments and access metadata must come from validated host context. Add explicit least-privilege allow rules and tests before integration.

Status: scaffold only; see the root ARCHITECTURE.md and implementation roadmap.
